<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Ubad.Views"
       x:Class="Ubad.AppShell"
       Shell.NavBarIsVisible="False">

    <Shell.Resources>
        <Style TargetType="TabBar">
            <Setter Property="Shell.TabBarBackgroundColor"  Value="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" />
            <Setter Property="Shell.TabBarForegroundColor"  Value="{StaticResource Primary}" />
            <Setter Property="Shell.TabBarUnselectedColor"  Value="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}" />
            <Setter Property="Shell.TabBarTitleColor"       Value="{StaticResource Primary}" />
        </Style>
    </Shell.Resources>

    <TabBar x:Name="MainTabBar" Shell.NavBarIsVisible="False">

        <Tab Title="Home" Icon="icon_home.png">
            <ShellContent Route="home" ContentTemplate="{DataTemplate views:HomePage}" />
        </Tab>

        <Tab Title="Projects" Icon="icon_projects.png">
            <ShellContent Route="projects" ContentTemplate="{DataTemplate views:ProjectsPage}" />
        </Tab>

        <Tab Title="Favorites" Icon="icon_favorites.png">
            <ShellContent Route="favorites" ContentTemplate="{DataTemplate views:FavoritesPage}" />
        </Tab>

        <Tab Title="Settings" Icon="icon_settings.png">
            <ShellContent Route="settings" ContentTemplate="{DataTemplate views:SettingsPage}" />
        </Tab>

    </TabBar>

</Shell>
